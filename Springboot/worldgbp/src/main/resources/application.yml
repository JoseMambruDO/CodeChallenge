
spring:
  application:
    name: 'worldgbp'
  datasource:
    driverClassName: org.postgresql.Driver
    password: postgres
    url: jdbc:postgresql://localhost:5432/worldgbp
    username: postgres
    
  flyway:
    password: postgres
    url: jdbc:postgresql://localhost:5432/worldgbp
    user: postgres
